.venv/
__pycache__/
*.py[cod]
*.sqlite3
*.sqlite3-journal
.env
.DS_Store
/media/
/staticfiles/
*.log
.idea/
.vscode/

# ห้าม commit ข้อสอบลิขสิทธิ์
/data/past_papers/
/data/exam_corpus/
*.pdf
.venv*/

# ── ไฟล์ระหว่างทางของ pipeline สร้างคำศัพท์ ไม่ต้องเก็บใน repo ──
# ผลลัพธ์สุดท้ายอยู่ใน hsk5_merged.json / needs_review.json / QUALITY_REPORT.md แล้ว
/data/vocab/batch_inputs/
/data/vocab/verify/
/data/vocab/hsk5_batch_*.json

# ข้อสอบต้นฉบับ (ลิขสิทธิ์)
/SampleExamQuestions/

# ไฟล์สำรอง — มีข้อมูลผู้เรียนจริง ห้าม commit
/backups/

# โมเดล TTS — ใหญ่ 211MB และดาวน์โหลดใหม่ได้ ไม่ต้องเก็บในรีโป
/data/tts/

# ไฟล์เสียงข้อฟัง — สร้างใหม่ได้ด้วย make_listening_audio จึงไม่เก็บใน git
# เก็บไว้ใน GitHub Release แทน แล้ว Dockerfile ดึงตอน build (ดู LISTENING_AUDIO_URL)
# เหตุผล: git เก็บทุกเวอร์ชันตลอดกาล ปรับเสียงทีนึงประวัติโต 50 MB ถาวร
/static/listening/
