# ประวัติเวอร์ชัน

รูปแบบเลข **ใหญ่.กลาง.เล็ก**

| ตำแหน่ง | เพิ่มเมื่อ | ตัวอย่าง |
|---|---|---|
| ใหญ่ | เปลี่ยนโครงระบบจนใช้ของเดิมต่อไม่ได้ | ย้ายไป HSK 3.0 |
| กลาง | มีของใหม่ให้ผู้เรียนใช้ | เพิ่มโหมดฝึกจำคำศัพท์ |
| เล็ก | แก้บั๊ก ปรับหน้าจอ ไม่มีของใหม่ | แก้บทอ่านไม่แสดง |

เลขที่กำลังให้บริการดูได้ที่ `/version/` หรือท้ายแถบเมนูซ้าย

---

## 1.14.1 — 4 ก.ย. 2026

**แก้ประโยคตัวอย่างที่จีนกับคำแปลไทยเป็นคนละเรื่องกัน**
- ผู้ใช้เจอเองที่ 与其 และ 此外 — ประโยคจีนพูดเรื่องหนึ่ง คำแปลไทยแปลอีกเรื่อง
- สาเหตุ: `seed_hsk5` เขียนทับ `example_zh` จากไฟล์ตั้งต้น แต่ไม่แตะ `example_th`
  และ `bootstrap` รัน seed **หลัง** `import_vocab` เสมอ กระทบราว 70 คำบน prod
- ซ่อนอยู่นานเพราะเครื่องพัฒนารันคำสั่งคนละลำดับกับเซิร์ฟเวอร์ — บนเครื่องผู้พัฒนา
  ข้อมูลถูกต้องมาตลอด ผู้ใช้จึงเป็นคนเจอ
- เพิ่มคำสั่ง `fix_vocab_examples` ซ่อมฐานที่พังไปแล้ว

**แก้ป้ายคำศัพท์ถูกล้างทุกครั้งที่รัน bootstrap** (ไม่มีใครรายงาน เจอตอนไล่โค้ด)
- seed เขียนทับ `tags` ทั้งก้อนด้วยแท็กเดียว → `human_verified` ที่ครูตรวจแล้ว
  หายหมด 94 คำทุกครั้งที่ deploy ร้ายแรงกว่าข้อบน
- ตอนนี้ seed เติมเฉพาะคำที่ยังไม่มี ไม่แตะของเดิมเลย

**เพิ่มปุ่มฟังซ้ำในการ์ดเฉลยข้อฟัง** — ผู้เรียนขอมาเอง จะได้อ่านบทไปฟังไปด้วย

**เอาไฟล์เสียงกลับเข้า git**
- เคยย้ายออกไป GitHub Release แต่ย้ายออกก่อนตั้งที่เก็บใหม่เสร็จ
  prod เลยไม่มีไฟล์เสียงเลยหนึ่งรอบ deploy ผู้เรียนได้ยินตัวอ่านของเบราว์เซอร์แทน
- ของที่ใช้งานได้สำคัญกว่ารีโปสะอาด กลไก `LISTENING_AUDIO_URL` ยังอยู่ เผื่อวันที่ย้ายจริง

---

## 1.14.0 — 3 ก.ย. 2026

**听写 ใช้ไฟล์เสียงแล้ว — ทุกเครื่องได้ยินเหมือนกัน**
- เดิม 听写 ใช้ตัวอ่านของเบราว์เซอร์อยู่หน้าเดียว แปลว่า Android ได้เสียงของ Google ·
  Windows ได้ของ Microsoft · บางเครื่องไม่มีเสียงจีนเลย
  ผู้เรียนสองคนฝึกประโยคเดียวกันแล้วได้ยินคนละอย่าง เทียบผลกันไม่ได้
- สร้างไฟล์รายประโยคเพิ่ม **724 ไฟล์** · รวมทั้งระบบ 949 ไฟล์ · 53 MB
- **แต่ละประโยคใช้เสียงของคนที่พูดประโยคนั้นจริง** — เดิมตัดประโยคจากบทที่ต่อกันแล้ว
  ซึ่งไม่รู้ว่าใครพูด ถ้าอัดแบบนั้นจะได้เสียงหญิงอ่านประโยคของชาย ผู้เรียนจะจำเสียงคำผิด

**เปลี่ยนเสียงผู้ชายให้ตรงกับผู้ประกาศตัวจริง**
- วัดระดับเสียงผู้ประกาศจากไฟล์ข้อสอบจริง: ชาย 132.3 Hz · หญิง 234.4 Hz
- สแกน Kokoro ทั้ง 103 เสียงหาตัวที่ใกล้ที่สุด → เปลี่ยนชายจาก sid 81 (111.1 Hz ต่างไป 21)
  เป็น **sid 95 (131.9 Hz ต่างไป 0.4)** · เสียงหญิง sid 28 ใกล้พออยู่แล้ว คงเดิม

**ตั้งใจไม่ปรับความดังให้เท่าต้นฉบับ**
วัดแล้วเราดังกว่า 1.44 เท่า แต่ห้องสอบมีเครื่องเสียง ส่วนผู้เรียนฟังจากลำโพงมือถือ
ลดให้เท่าต้นฉบับจะเบาเกินจนฟังไม่ชัด — เลียนแบบตรงนี้ทำให้ของแย่ลง ไม่ใช่ดีขึ้น

---

## 1.13.0 — 3 ก.ย. 2026

**ปรับความเร็วและจังหวะเสียงตามข้อสอบจริง — วัดจากไฟล์เสียง H51001 ของจริง**
- **เสียงเดิมพูดเร็วกว่าข้อสอบจริง 32%** ซึ่งเป็นเหตุผลที่ฟังยาก
  ต้นฉบับ 3.43 ตัวอักษร/วินาที · ของเราเดิม 4.52
- พาร์ท 1 ช้าลงเป็น 0.71× · พาร์ท 2 เป็น 0.81× —
  ข้อสอบจริงพาร์ท 1 อ่านช้ากว่าพาร์ท 2 ชัดเจน ใช้ความเร็วเดียวทั้งฉบับทำให้พาร์ท 1 ยากเกินจริง
- ช่วงเว้นระหว่างคนพูด 0.38 → 0.55 วินาที · ก่อนคำถาม 0.85 → 0.90
  (ต้นฉบับวัดได้ กลาง 0.58 · 75% ที่ 0.88)
- วัดซ้ำหลังสร้างใหม่: พาร์ท 1 ได้ 3.10 · พาร์ท 2 ได้ 3.58 — คลาดจากเป้า 2-3%
- ปุ่ม "เท่าข้อสอบ" ตอนนี้**เท่าข้อสอบจริงแล้ว** ก่อนหน้านี้ป้ายบอกผิดมาตลอด

**ไม่ใส่ช่วงให้ฝนคำตอบ 16.2 วินาทีโดยตั้งใจ**
ข้อสอบต้องมีเพราะเทปหยุดไม่ได้ แต่ระบบเราให้ผู้เรียนกดเองว่าจะไปข้อต่อไปเมื่อไหร่
ใส่ไปจะกลายเป็นเวลาตายที่ต้องนั่งรอเปล่าๆ 12 นาทีต่อชุด

---

## 1.12.0 — 3 ก.ย. 2026

**เสียงข้อฟังเปลี่ยนเป็นไฟล์ที่อัดไว้ล่วงหน้าด้วย Kokoro**
- 225 ไฟล์ · 105 นาที · 26 MB — เจ้าของระบบฟังเทียบ 12 เสียงแล้วเลือกเอง
  หญิง sid 28 · ชาย sid 81 (ห่างกัน 117 Hz มากพอให้แยกออกว่าใครพูด)
- ดีขึ้น 3 อย่าง: เสียงเป็นธรรมชาติกว่ามาก · **ทุกคนได้ยินเหมือนกัน**ไม่ว่าเปิดจากเครื่องอะไร ·
  มีผู้พูดสองคนจริง ไม่ต้องปลอมด้วยการลดระดับเสียง
- บทเล่าเรื่องสลับเพศผู้บรรยายตามเลขข้อ — ข้อสอบจริงใช้ทั้งชายและหญิง
  ถ้าซ้อมแต่เสียงเดียว เจอผู้บรรยายชายในห้องสอบจะฟังยากขึ้นทันที
- ปรับความเร็วยังทำได้ (playbackRate ไม่ทำให้เสียงเพี้ยน) และเปลี่ยนได้ระหว่างเล่น
- **ถอยได้ทุกชั้น** — ข้อที่ไม่มีไฟล์ · โหลดไม่สำเร็จ · เน็ตหลุด → กลับไปใช้ตัวอ่านของเบราว์เซอร์
- Kokoro เป็น Apache-2.0 ทั้งโค้ดและ weights รันในเครื่องเราเอง บทไม่ออกไปไหน
  ⚠️ แต่เสียงที่ได้แปลงจากบทข้อสอบจริง จึงติดลิขสิทธิ์เหมือนตัวบท (D6)

---

## 1.11.0 — 2 ก.ย. 2026

**เสียงข้อฟังเล่นทีละคน มีจังหวะเว้น เหมือนห้องสอบจริง**
- เดิมต่อบทสนทนาทั้งข้อเป็นก้อนเดียวให้เสียงเดียวอ่านรวดเดียวไม่หยุด
  ซึ่ง **ฟังยากกว่าของจริง** ไม่ใช่แค่ไม่เหมือน — ผู้เรียนแยกไม่ออกว่าประโยคไหนเป็นของใคร
  แล้วคำถามอย่าง 男的是什么意思？ ตอบไม่ได้ทั้งที่ฟังออกครบทุกคำ
- ตอนนี้เล่นทีละช่วง เว้น 0.38 วินาทีระหว่างคนพูด และ **0.85 วินาทีก่อนอ่านคำถาม**
  ผู้เรียนจะรู้ว่าคำถามเริ่มตรงไหน
- ตาผู้ชายพูดลดระดับเสียงลง ไม่ได้ทำให้เหมือนผู้ชายจริง แต่ทำให้ *แยกออกว่าเปลี่ยนคนพูดแล้ว*
  ซึ่งคือสิ่งที่คำถามประเภทนี้ต้องใช้ (เครื่องส่วนใหญ่มีเสียงจีนใช้ได้จริงตัวเดียว)
- คำถามท้ายข้ออ่านช้าลง 6% เพราะเป็นเสียงผู้บรรยาย ไม่ใช่คู่สนทนา

---

## 1.10.3 — 2 ก.ย. 2026

**แก้: เสียงข้อฟังฟังไม่รู้เรื่อง**
- โค้ดเดิมหยิบเสียงจีน *ตัวแรกที่เจอ* ซึ่งบน macOS/iOS คือ Eddy / Flo / Grandma
  ที่เป็น "เสียงเล่น" ของ Apple ออกแบบมาให้ตลก ไม่ได้ออกแบบมาให้ฟังชัด
  ตอนนี้เรียงใหม่แล้วได้ **Tingting** ซึ่งเป็นเสียงจีนกลางมาตรฐานของระบบ
- เพิ่มตัวเลือกให้เปลี่ยนเสียงเองได้ พร้อมปุ่ม **ลองฟังเสียงนี้** จำไว้ทุกหน้า
- เลือกเสียงที่อยู่ในเครื่องก่อนเสียงจากคลาวด์เสมอ — เสียงคลาวด์แปลว่า
  บทพูดถูกส่งไปสังเคราะห์ที่เซิร์ฟเวอร์ของผู้ผลิตเบราว์เซอร์ ซึ่งเป็นข้อความลิขสิทธิ์ (D6)
  ถ้าเลี่ยงไม่ได้ ระบบจะเตือนบนหน้าจอว่ากำลังใช้เสียงคลาวด์อยู่

---

## 1.10.2 — 2 ก.ย. 2026

**แก้: `import_listening` ใช้บนเซิร์ฟเวอร์ไม่ได้**
- `data/exam_corpus/` อยู่ทั้งใน .gitignore และ .dockerignore จึงไม่ติดไปกับ image
  คำสั่งบนเซิร์ฟเวอร์เลยหาบทถอดเสียงไม่เจอและไม่ทำอะไรเลย
- เพิ่ม `export_listening` (รันบนเครื่องพัฒนา) ทำไฟล์ `data/listening_fixture.json`
  แล้ว `import_listening` อ่านไฟล์นั้นแทนเมื่อไม่พบต้นฉบับ
- ถ้ามีทั้งสองแหล่ง ใช้ต้นฉบับก่อนเสมอ และบอกบนจอว่าอ่านจากไหน

---

## 1.10.1 — 2 ก.ย. 2026

**ย้ายสวิตช์พินอินขึ้นแถบบน**
- เดิมอยู่ท้ายแถบเมนูซ้าย บนมือถือต้องเปิดแผ่นเมนูก่อนทุกครั้ง = สามจังหวะต่อหนึ่งครั้งที่กด
  ทั้งที่เป็นปุ่มที่กดบ่อยที่สุดในระบบ (เดาก่อน → เปิดดูพินอิน → ปิดใหม่ ทุกข้อ)
- ตอนนี้อยู่มุมขวาบนของทุกหน้า กดทีเดียว ไม่ต้องเปิดเมนู
- แถบบนขึ้นเสมอแม้บัญชีนั้นไม่มีวันสอบ ไม่งั้นสวิตช์จะหายไปสำหรับบัญชีผู้ดูแล
- จอแคบตัดคำว่า "พินอิน" ทิ้ง เหลือไอคอนกับสถานะ เพื่อไม่ให้เบียดชิปนับถอยหลัง

---

## 1.10.0 — 1 ก.ย. 2026

**วัดผลพาร์ทฟัง** (`/listen/test/`)
- เลือกชุด 5 / 10 / 45 ข้อ · จับเวลา · **ฟังได้ครั้งเดียวเหมือนห้องสอบ** · ไม่เฉลยจนจบ
- ชุดสั้นติดป้ายว่า "ซ้อม" และไม่แปลงเป็นคะแนนเต็ม 100 —
  ชุด 5 ข้อ หนึ่งข้อคิดเป็น 20% ของผล เดาถูกข้อเดียวตัวเลขก็เด้ง 20 จุด
  เฉพาะชุด 45 ข้อเท่านั้นที่นับเป็นคะแนนและเข้าประวัติแนวโน้ม

**ข้อ 100 เขียนจากภาพ (看图写作)**
- ภาพโจทย์ 10 ฉาก วาดเองด้วย SVG — เป็นของเรา 100% ไม่ติดลิขสิทธิ์ ไม่ต้องต่อเน็ต
- ในภาพไม่มีตัวอักษรเลย (มีเทสต์ล็อกไว้) — ใส่ตัวจีนคือให้ศัพท์ฟรี ใส่ตัวไทยคือบอกคำตอบ
- คำบรรยายฉากส่งไปกับพรอมต์ตอนขอตรวจ เพราะผลตรวจส่งกลับเป็นข้อความ
  ผู้ตรวจไม่เห็นภาพ ถ้าไม่บอกจะตัดสิน 内容与图片相关 ไม่ได้

**หน้าให้ครูตรวจคำศัพท์** (`/vocab/teacher/`)
- เรียงตาม "โอกาสที่จะผิด × ราคาของการผิด" ไม่ใช่ความถี่ —
  很 是 不 ออกทุกชุดก็จริง แต่คำแปลไม่มีทางผิด การให้ครูไล่ดูคือเผาเวลาครูทิ้ง
- ครูไม่ต้องมีบัญชี · กด "แปลผิด" แล้วพิมพ์คำแปลใหม่ จะเขียนทับของ AI จริง

**แก้วันสอบได้จากหน้าจัดการผู้ใช้** — เดิมต้องเข้าหน้าจัดการข้อมูลของ Django

---

## 1.9.0 — 1 ก.ย. 2026

**พาร์ทฟังใช้งานได้แล้ว — 100 คะแนนจาก 300 ที่ระบบเคยแตะไม่ได้เลย**
- ปลดล็อกข้อสอบฟัง **225 ข้อ** จาก 5 ชุดที่มีบทถอดเสียง (อีก 4 ชุดไม่มีบท ยังปิดอยู่)
- เสียงสร้างที่เครื่องผู้เรียนด้วยตัวอ่านของเบราว์เซอร์ ปรับความเร็วได้ 3 ระดับ
  ไม่มีไฟล์เสียง ไม่มีกุญแจ API ไม่มีค่าใช้จ่ายต่อครั้ง
- บทพูดไม่ได้อยู่ในหน้าเว็บ ดึงมาตอนกดฟัง — ถ้าฝังไว้ ผู้เรียนกดดูซอร์สก็อ่านคำตอบได้
- นับจำนวนครั้งที่กดฟัง และแสดง **"ถูกตั้งแต่ฟังรอบเดียว"** คู่กับความแม่นรวมเสมอ
  เพราะในห้องสอบไม่มีปุ่มฟังซ้ำ ตัวเลขความแม่นรวมมองโลกในแง่ดีเกินไปเสมอ
- ตอบผิดข้อฟังติดป้ายสาเหตุ **SOUND** ซึ่งเป็นรหัสที่ไม่เคยถูกใช้เลยตั้งแต่ทำระบบมา

**听写 ฟังแล้วพิมพ์ตาม — 724 ประโยค**
- เทียบทีละตัวอักษร แยกสี่ชนิด: ถูก · เสียงพ้อง · ตัวผิด · ตกไป · เกินมา
- ป้าย "เสียงพ้อง" ติดต่อเมื่อพิสูจน์ได้จากพินอินในคลังของเราเอง ไม่เดาจากรูปตัวอักษร
  เพราะฟังออกแล้วเขียนผิดตัว กับฟังไม่ออก เป็นคนละปัญหาที่แก้คนละวิธี
- ไม่นับเครื่องหมายวรรคตอน — คนที่ฟังออกครบไม่ควรถูกหักเพราะไม่ได้ใส่จุลภาค

**ชี้ประโยคที่มีคำตอบ แทนคำอธิบายที่ AI แต่งขึ้น**
- ข้อฟังผิดเพราะฟังไม่ทัน ไม่ใช่เพราะไม่เข้าใจเหตุผล สิ่งที่ช่วยได้คือชี้ว่าพลาดตรงไหน
- ทำได้ 171 จาก 225 ข้อ ที่เหลือคือข้อที่คำตอบเป็นการตีความ ซึ่งระบบบอกตรงๆ ไม่ใช่เดา

**สรุปสัปดาห์**
- เทียบ 7 วันล่าสุดกับ 7 วันก่อนหน้าทุกตัวเลข ไม่แสดงตัวเลขเดี่ยว
- แยกสาเหตุที่ผิดตาม D8 และให้ข้อเสนอที่อ้างตัวเลขบนหน้าเดียวกัน
- เจ้าของระบบสลับดูของแต่ละคนได้จากหน้าเดียว

**ติดตั้งลงจอโฮมได้ (PWA)**
- กด "เพิ่มลงจอโฮม" แล้วเปิดเหมือนแอป มีทางลัดไปชุดฝึกวันนี้ / ฝึกฟัง / ฝึกจำคำศัพท์
- ตั้งใจไม่แคชหน้าเว็บ แคชเฉพาะ CSS/JS/ไอคอน เพราะพี่น้องใช้เครื่องเดียวกัน
  ถ้าแคชหน้าไว้ คนหนึ่งจะเห็นหน้าของอีกคน

---

## 1.8.0 — 31 ส.ค. 2026

**ส่งงานเขียนให้เจ้าของระบบตรวจ แทนการยิงเข้า AI ตรง**
- ผู้เรียนกด "ส่งให้เจ้าของระบบตรวจ" ฝากข้อความบอกได้ว่าอยากให้ดูตรงไหน
- เจ้าของระบบมีเมนู **งานเขียนรอตรวจ** พร้อมตัวเลขว่ามีกี่ชิ้นค้างอยู่
  กดคัดลอกพรอมต์ไปถาม Claude เอง แล้ววาง JSON ที่ได้กลับมา
- ทำไมเปลี่ยน: ระบบไม่ต้องเก็บกุญแจ API เลย (ไม่มีอะไรให้หลุด) ไม่มีค่าใช้จ่ายต่อครั้ง
  และเจ้าของระบบได้อ่านผลก่อนผู้เรียน ถ้าตอบมั่วก็ไม่ต้องวางลง
- แลกกับการที่ผู้เรียนต้องรอ และเจ้าของระบบกลายเป็นคอขวด
- ทางเรียก API ตรงยังอยู่ครบ ปิดไว้ — ตั้ง `ANTHROPIC_API_KEY` เมื่อไหร่ก็เปิดใช้ได้ทันที
  ทั้งสองทางเขียนผลลงที่เดียวกัน ปุ่มแย้งและการตัดสินระดับทำงานเหมือนกัน
- ผลตรวจขึ้นในหน้าประวัติของวันที่เขียนด้วย ไม่ใช่แค่หน้างานชิ้นนั้น

**ความยินยอมอยู่ข้ามวันแล้ว**
- เดิมเก็บใน session ซึ่งหายทุกเที่ยงคืนตอนระบบดีดออก ผู้เรียนจึงเจอแบนเนอร์ทุกวัน
  จนกลายเป็นการกดผ่านโดยไม่อ่าน — ตรงข้ามกับจุดประสงค์ของการขอความยินยอม
- ย้ายไปเก็บในโปรไฟล์เป็นวันเวลา และมีปุ่มถอนความยินยอมที่หน้าเขียน
- กดยินยอมได้จากหน้าผลตรวจเลย ไม่ต้องเดินอ้อมกลับไปหน้าเขียน

---

## 1.7.0 — 31 ส.ค. 2026

**หน้าเขียนเรียงความ 书写第二部分 (ข้อ 99)**
- โจทย์สุ่มคำ 5 คำจากคลังของเราเอง ไม่ใช้โจทย์ลิขสิทธิ์ · ชิปคำเปลี่ยนสีทันทีที่พิมพ์คำนั้น
- Claude ตรวจให้ผ่าน API แล้วชี้ว่าผิดตรงไหน เพราะอะไร เป็นภาษาไทย
- ทุกจุดที่ AI บอกว่าผิดมีปุ่มแย้ง เก็บลงฐานข้อมูล
- ต้องกดยินยอมก่อนใช้ครั้งแรก เพราะงานเขียนถูกส่งออกนอกระบบ

**รูปพินอินที่ออกเสียงจริง 13 คำ**
- คำที่มี 一 หรือ 不 แสดงสองรูปคู่กัน พร้อมอธิบายว่าทำไมมีสองรูป
- ค่ามาจากลิสต์คำศัพท์ทางการ HSK 2012 ไม่ได้คำนวณเองและไม่ได้มาจาก AI

---

## 1.6.0 — 31 ส.ค. 2026

**ฝึกเรียงคำนับเป็นงานแล้ว**
- เดิมบันทึกเฉพาะตอนตอบผิด คนที่เรียงถูกหมดจึงขึ้นในหน้ากลุ่มว่า "ไม่ได้ทำอะไร"
  ทั้งที่ 书写第一部分 คือ 8 ข้อจาก 10 ของพาร์ทเขียน
- เรียงถูกแล้วปิดบันทึกข้อผิดเดิมของข้อนั้นด้วย

**ป้องกันข้อมูลหาย**
- ลบคำศัพท์จากหน้า admin ไม่ได้แล้ว — ลบหนึ่งคำ = ลบการ์ดและประวัติ
  ของทุกคนที่ผูกกับคำนั้นไปด้วย และกู้คืนไม่ได้

---

## 1.5.0 — 31 ส.ค. 2026

**ข้อสอบจำลองตรงกับของจริงขึ้น**
- เดิมสุ่มรายข้อ ชุด 45 ข้อจึงกระจายอยู่ใน 30 บทอ่าน (1.13 ข้อต่อบท)
  ทั้งที่ข้อสอบจริงมีราว 4 ข้อต่อบท = ต้องอ่านหนักกว่าของจริงเกือบสามเท่าในเวลาเท่ากัน
  ตอนนี้หยิบทั้งบท เหลือ 10-12 บทต่อชุด

**ผู้เรียนเปลี่ยนรหัสผ่านเองได้แล้ว**
- ค้างมาตั้งแต่ขึ้นเซิร์ฟเวอร์ · เข้าจากหน้าโปรไฟล์ · มีปุ่มดูรหัส
- ต้องกรอกรหัสเดิมก่อน กันคนยืมเครื่องที่เปิดค้างไว้

**หน้าที่เคยพัง 500 สำหรับบัญชีผู้ดูแล**
- 23 view เรียกโปรไฟล์ผู้เรียนแล้วใช้ต่อทันทีโดยไม่เช็คค่าว่าง
  บัญชีผู้ดูแลไม่มีโปรไฟล์ จึงพังทุกหน้ายกเว้นหน้าแรก
- เพิ่มเทสต์กวาดทุก URL ในระบบ จะจับหน้าใหม่ที่เพิ่มทีหลังด้วยอัตโนมัติ

**อื่นๆ**
- เริ่มสอบจำลองตอนคลังข้อสอบไม่พอ เดิมพังที่หน้าทำข้อสอบ ตอนนี้บอกเหตุผลแทน

---

## 1.4.0 — 31 ส.ค. 2026

รอบแก้ตามผลตรวจระบบด้วยหกบทบาท (รายงานเต็มอยู่ที่ `docs/system-review-2026-08-31.md`)

**ป้ายสาเหตุการตอบผิด — D8 ที่เป็นเหตุผลหลักของระบบ**
- เดิมติดป้ายตายตัว: ข้อคำศัพท์ได้ VOCAB เสมอ ข้ออื่นได้ MEANING เสมอ
  รหัส TOO_SLOW / STRUCTURE / SOUND จึงไม่เคยถูกเขียนลงฐานเลยสักครั้ง
- ตอนนี้เดาจากชนิดโจทย์ พาร์ท และเวลาที่ใช้จริง

**ข้อที่ตอบถูกแล้วต้องหลุดออกจากคิว**
- เดิมปิดบันทึกข้อผิดเฉพาะข้อคำศัพท์ ข้อสอบจึงค้างในคิว 30% ตลอดกาล
  ต่อให้ตอบถูกกี่ครั้ง และตัวเลข "ข้อที่ยังผิดค้าง" ขึ้นทางเดียว

**คะแนนสอบจำลองพูดความจริง**
- `passed` เดิมคืน "ไม่ผ่าน" เสมอ เพราะรวมสามพาร์ทแต่ระบบวัดได้พาร์ทเดียว
  ตอนนี้คืน "ยังวัดไม่ครบ" และหน้าสรุปบอกตรงๆ ว่านี่ไม่ใช่คะแนนสอบจริง

**ความปลอดภัยและการกู้คืน**
- `DJANGO_DEBUG` ตั้งต้นเป็นปิด และไม่ยอมสตาร์ทถ้าไม่ตั้ง `DJANGO_SECRET_KEY`
- คำสั่ง `backup` สำรองทั้งฐาน — ทดสอบกู้คืนใส่ฐานเปล่าแล้วว่าได้จริง 15,782 รายการ

**อื่นๆ**
- ปุ่ม "เฉลยนี้ผิด บอกเรา" เดิมกดแล้วไม่เกิดอะไร ตอนนี้ต่อกับฟอร์มแย้งจริง
- คำอธิบาย 145 ชุดที่ระบบตั้งธงว่าน่าสงสัยขึ้นป้ายเตือนแล้ว (เดิมไม่มีที่ไหนอ่านธง)
- ช่อง "ทวนเสริม" ไม่ดันการ์ดที่ยังไม่เคยเรียนเข้ามาอีก และชุดที่สั้นกว่าเป้าบอกเหตุผล
- คำสั่ง `suspend_bare_questions` พักข้อที่ไม่มีคำอธิบายเลย 8 ข้อ

---

## 1.3.0 — 31 ส.ค. 2026

**เซสชันหมดอายุตอนเที่ยงคืน**
- ข้ามวันแล้วต้องเข้าสู่ระบบใหม่ เพื่อให้เห็นว่าผู้เรียนเข้ามาใหม่ทุกวันจริงไหม
- เตะออกแล้วพากลับไปหน้าที่ตั้งใจเข้า ไม่ใช่เด้งกลับหน้าแรกทุกครั้ง
- ปิดได้ด้วย `DJANGO_DAILY_LOGOUT=0` โดยที่สถิติวันเข้าระบบยังครบเหมือนเดิม

**บันทึกวันเข้าระบบ**
- ตาราง LoginDay หนึ่งแถวต่อคนต่อวัน — ตอบได้ว่า "เข้ามากี่วันใน 30 วัน"
  ซึ่ง User.last_login เดิมตอบไม่ได้ เพราะเก็บแค่ครั้งล่าสุด
- หน้าความคืบหน้ากลุ่มเพิ่มระดับที่สี่ในปฏิทิน: เข้ามาแต่ไม่ได้ทำอะไร
  ต่างจากวันที่ไม่ได้เข้าเลย เป็นสัญญาณคนละแบบ

---

## 1.2.0 — 31 ส.ค. 2026

**นับถอยหลังวันสอบบนทุกหน้า**
- แถบมุมซ้ายบนบอกว่าเหลือกี่วัน ทั้งรอบแรกและรอบสำรอง
- ยิ่งใกล้ยิ่งเปลี่ยนสี — เหลือ 30 วันเป็นเหลือง เหลือ 14 วัน (ช่วง freeze) เป็นแดง
- ซ่อนตอนจำลองสอบ เพราะห้ามมีอะไรกวนตอนจับเวลา

**หน้าความคืบหน้ากลุ่ม**
- ชี้หรือแตะช่องปฏิทินแล้วบอกวันที่และสิ่งที่ทำวันนั้น
  เดิมใช้ tooltip ของ HTML ซึ่งบนแท็บเล็ตไม่ขึ้นเลยเพราะไม่มีการชี้เมาส์
- ปฏิทินแยกสามระดับ: ทำชุดหลัก · ทำอย่างอื่น · ไม่ได้ทำ
  เดิมวันที่ทวนคำศัพท์อย่างเดียวขึ้นสีเทาเหมือนวันที่ไม่ได้แตะระบบเลย
- เพิ่มรายการว่าแต่ละคนใช้ส่วนไหนของระบบไปแล้วบ้าง
  ชุดฝึกวันนี้ · ฝึกจำคำศัพท์ · จำลองสอบ · แบบวัดระดับ · เข้าระบบล่าสุด

---

## 1.1.1 — 28 ส.ค. 2026

- คำสำคัญท้ายคำอธิบายเฉลยมีพินอินแล้ว ทั้งในชุดฝึกรายวันและหน้าฝึกเรียงคำ
  จังหวะที่เพิ่งตอบผิดคือจังหวะที่อยากรู้ที่สุดว่าคำนี้อ่านว่าอะไร
  เดิมต้องออกไปเปิดพจนานุกรมเอง
- เตรียมพินอินของคำนอกคลัง HSK5 ไว้ล่วงหน้า 773 คำ (สำนวน · คำ HSK6 · คำประสม)
  ครอบคลุมคำสำคัญครบ 1,402 คำ จากเดิมมีพินอินแค่ 34%
- พินอินส่วนนี้ปิด/เปิดตามสวิตช์กลางเหมือนที่อื่นทั้งเว็บ

---

## 1.1.0 — 28 ส.ค. 2026

**เปลี่ยนพฤติกรรมของชุดฝึกรายวัน**
- ข้อสอบจริงในชุดวันละไม่เกิน 10 ข้อ ที่เหลือเป็นคำศัพท์ทั้งหมด
  ชุดฝึกมีหน้าที่สร้างฐานคำศัพท์ซึ่งเป็นคอขวดจริง
  ส่วนการฝึกทำข้อสอบเต็มรูปแบบอยู่ที่โหมดจำลองสอบสัปดาห์ละครั้ง

**พินอิน**
- สวิตช์ปิด/เปิดพินอินตัวเดียวคุมทั้งเว็บ อยู่ท้ายแถบเมนู (มือถืออยู่ในเมนูเพิ่มเติม)
- **ค่าตั้งต้นคือปิด** — ตราบใดที่พินอินอยู่ข้างๆ ตาจะไปเกาะพินอินก่อน
  กลายเป็นอ่านพินอินไม่ใช่อ่านตัวอักษร แต่วันสอบจริงไม่มีพินอินให้

**เมนู**
- ย้าย "ฝึกจำคำศัพท์" เข้ากลุ่ม "ฝึก" อยู่คู่กับ "วันนี้"

---

## 1.0.0 — 28 ส.ค. 2026

เวอร์ชันแรกที่ขึ้นเซิร์ฟเวอร์จริงและผู้เรียนใช้งานได้ครบวงจร

**สิ่งที่ผู้เรียนใช้ได้**
- ชุดฝึกรายวัน 40 ข้อ คละพาร์ท วันละครั้ง
- โหมดฝึกจำคำศัพท์ — ดูคำก่อน แล้วค่อยทดสอบ
- คลังคำศัพท์ HSK1-6 · ปิดตัวอักษร / พินอิน / คำแปลแยกกันได้
- ฝึกเรียงคำ · จำลองสอบจับเวลา · เก็งข้อสอบ · คำเชื่อม
- สถิติ · ประวัติรายวัน · ความคืบหน้ากลุ่ม

**ข้อมูล**
- คำศัพท์ 2,206 คำ (กันไว้ 305 คำที่รอครูตรวจ)
- ข้อสอบจริง 924 ข้อ · คำอธิบายเฉลย 4 ชั้น 492 ชุด
- กลุ่มคำใกล้เคียงจากตัวเลือกจริง 106 กลุ่ม

**บั๊กที่แก้ก่อนออกเวอร์ชันนี้**
- ชุดฝึกรายวันไม่แสดงบทอ่าน ทำให้ 315 ข้อตอบไม่ได้ (ผู้เรียนแจ้ง)
- ข้อเติมคำไม่บอกว่ากำลังตอบช่องไหน 135 ข้อ (ผู้เรียนแจ้ง)
- ข้อ 阅读第二部分 ดูเหมือนคำถามหาย (ผู้เรียนแจ้ง)
- โหมดฝึกจำคำศัพท์มีคำให้ฝึกแค่ที่เคยเจอแล้ว
- หน้าเข้าสู่ระบบบอกสาเหตุผิด และไม่มีปุ่มดูรหัสผ่าน
- แถบเมนูซ้ายตกขอบจอเตี้ย กดออกจากระบบไม่ได้
