# HSK5 Trainer — บริบทโปรเจกต์

> ไฟล์นี้คือความจำของโปรเจกต์ อ่านก่อนเริ่มทำงานทุกครั้ง
> อัปเดตทุกครั้งที่มีการตัดสินใจใหม่ ห้ามให้ล้าสมัย

---

## 1. โปรเจกต์นี้คืออะไร

ระบบฝึกสอบ **HSK ระดับ 5** สำหรับผู้เรียนไทย

- **ผู้สร้าง / เจ้าของ:** Supakit (dev, ไม่ได้เป็นคนสอบเอง)
- **ผู้ใช้คนแรก:** น้องของเจ้าของ — กำลังเตรียมสอบเต็มเวลา มีครูสอนอยู่แล้ว มีหนังสือเตรียมสอบและข้อสอบเก่า
- **เป้าหมายระยะสั้น:** น้องสอบ HSK5 ผ่าน (180/300) ในปี 2026
- **เป้าหมายระยะยาว:** ทำเป็นสินค้า — น้องคือผู้ใช้คนที่ 1 ไม่ใช่ผู้ใช้คนเดียว

**ระบบนี้ไม่ได้มาแทนครู** — ครูสอนเนื้อหา ระบบนี้ทำสิ่งที่ครูไม่ได้ทำ:
การฝึกซ้ำรายวันระหว่างคาบ · การวัดผลที่เป็นตัวเลข · การบอกว่า *ผิดเพราะอะไร* ไม่ใช่แค่ผิดกี่ข้อ

---

## 2. ข้อเท็จจริงเรื่องข้อสอบ (ยืนยันจากการค้นเว็บ ส.ค. 2026)

**มาตรฐานที่ใช้: HSK 2.0** — HSK 3.0 ประกาศ syllabus พ.ย. 2025 ระบุ implement ก.ค. 2026
แต่ ณ 9 ส.ค. 2026 การสอบ HSK 1-6 รอบปกติ **ยังใช้ 2.0** ยังไม่มีประกาศเปลี่ยนอย่างเป็นทางการ
→ ระบบต้องรองรับทั้งสองมาตรฐานผ่านตาราง `ExamSpec` ห้าม hard-code

| | HSK5 (2.0) | HSK5 (3.0 ถ้าเปลี่ยน) |
|---|---|---|
| คำศัพท์สะสม | 2,500 | ~3,600 |
| 听力 ฟัง | 45 ข้อ ~30 นาที | 35 ข้อ ~25 นาที |
| 阅读 อ่าน | 45 ข้อ 45 นาที (ข้อ 46-90) | 35 ข้อ 35 นาที |
| 书写 เขียน | 10 ข้อ 40 นาที (8 ข้อเรียงคำ + 2 เรียงความ ~80 อักษร) | 2 เรียงความ (4 ภาพ ≥100 + เรียงความ ≥200) |
| คะแนน | เต็ม 300 · **ผ่าน 180 · ไม่มีขั้นต่ำรายพาร์ท** | เหมือนกัน |

เลขข้อ HSK5 2.0: 听力 1-45 · 阅读 46-90 (46-60 / 61-70 / 71-90) · 书写 91-100

**วันสอบ 2026:** 7 พ.ย. (paper+iBT, ปิดรับ paper 11 ต.ค. / iBT 28 ต.ค.) · 22 พ.ย. (iBT, ปิดรับ 8 พ.ย.) · 13 ธ.ค. (paper+iBT, ปิดรับ paper 16 พ.ย. / iBT 3 ธ.ค.)

**ต้องสอบ iBT ไม่ใช่ paper** เพราะ (ก) ผล iBT รอบ 7 พ.ย. ออก ~21 พ.ย. ทันเส้นตายสมัคร iBT 13 ธ.ค. ส่วน paper ออก 7 ธ.ค. ไม่ทัน (ข) iBT พิมพ์ pinyin ไม่ต้องเขียนตัวอักษรด้วยมือ ประหยัด 30-45 ชม.

---

## 3. การตัดสินใจที่ล็อกแล้ว

| # | ตัดสินใจ | เหตุผล |
|---|---|---|
| D1 | มาตรฐาน 2.0 เป็นค่าตั้งต้น แต่เก็บ `standard` เป็น field ทุกตารางที่มีเนื้อหา | HSK 3.0 อาจมาเมื่อไหร่ก็ได้ · เพิ่มทีหลัง audit ไม่ได้ |
| D2 | Multi-user + role ตั้งแต่ migration แรก | ตั้งใจทำเป็นสินค้า · ต้นทุนตอนนี้ ~2 ชม. ตอนหลัง ~30 ชม. |
| D3 | Django 5.2 LTS + HTMX + Alpine.js **ไม่มี React** | หน้าจอไม่กี่หน้า · Django admin ฟรี · ลด scope creep |
| D4 | SQLite ตอน dev, Postgres ตอน prod (สลับด้วย `DATABASE_URL`) | เร็วตอนพัฒนา พร้อม deploy |
| D5 | Deploy ขึ้น VPS ให้ใช้บนมือถือได้ | ผู้เรียนต้องใช้ตอนเดินทาง |
| D6 | ทุกตารางที่มีเนื้อหาต้องมี `source_type` + `commercial_safe` | มีข้อสอบเก่าลิขสิทธิ์อยู่ในเครื่อง ห้ามหลุดเข้าเวอร์ชันขาย |
| D7 | ชุดข้อสอบรายวัน **ขนาดคงที่** ไม่โตแบบทบต้น | กฎ +10%/วัน ที่เจ้าของเสนอ ทำให้ชนเพดานเวลาวันที่ 33 และถึง 324,940 ข้อวันสอบ |
| D8 | ทุกครั้งที่ตอบผิดต้องติดป้าย **สาเหตุ** | นี่คือสิ่งที่ Anki/Quizlet/Du Chinese ไม่มี = เหตุผลเดียวที่ระบบนี้ควรมีอยู่ |
| D9 | มี coach dashboard สำหรับเจ้าของ/ครู | คนเรียนกับคนสร้างเป็นคนละคน ต้องเห็นว่าน้องทำจริงไหม |
| D10 | เว็บก่อน (Django templates + HTMX + Alpine, ไม่มี build step) · แอปในสโตร์ค่อยทำด้วย Expo ต่อ API เดิม | ผู้เรียน 2 คน เหลือเวลาถึงวันสอบไม่มาก · ตรรกะอยู่ใน srs.py/selection.py อยู่แล้ว เปิด API ครอบทีหลังได้ไม่ต้องเขียนใหม่ |
| D11 | ตรวจเรียงความผ่าน **การถ่ายทอด** — ผู้เรียนกดขอ เจ้าของระบบก๊อปพรอมต์ไปถาม Claude เอง แล้ววาง JSON กลับ · โค้ดต่อ API ตรงยังอยู่ ปิดไว้ เปิดด้วย `ANTHROPIC_API_KEY` | ไม่มีกุญแจในระบบ = ไม่มีอะไรให้หลุด · ไม่มีค่าใช้จ่ายต่อครั้ง · เจ้าของได้อ่านผลก่อนผู้เรียน ถ้าตอบมั่วก็ไม่ต้องวางลง |

---

## 4. Daily Drill Engine — กฎหลักของระบบ

ขนาดชุดคงที่ (ค่าตั้งต้น 40 ข้อ ≈ 45 นาที) **ส่วนผสม** เปลี่ยนทุกวัน:

| ส่วน | สัดส่วน | เลือกจาก |
|---|---|---|
| ถึงกำหนดทบทวน | 50% | คิว SRS เรียงตามความน่าจะลืมมากสุด |
| เคยตอบผิด | 30% | `ErrorLog` เรียงตาม `miss_count` × ความใหม่ |
| ของใหม่วันนี้ | 20% | คำใหม่ตามโควตารายสัปดาห์ |

- **ความยากโตแทนจำนวนข้อ** — W1-4 ประโยคสั้น · W5-8 ความยาวเท่าข้อสอบจริง · W9+ จับเวลาโหดกว่าจริง 10%
- **คละพาร์ทในชุดเดียว (interleaving)** — จำได้นานกว่าทำทีละกอง แม้คะแนนซ้อมจะแย่กว่า
- **ตัวคุมโหลด** — ของค้างเกินเพดาน → หยุดคำใหม่วันนั้นอัตโนมัติ (สาเหตุอันดับ 1 ที่คนเลิกกลางคัน)
- **Freeze** — 14-15 วันก่อนสอบ ห้ามคำใหม่ ทบทวนล้วน ให้การ์ดทุกใบผ่านสายตาอย่างน้อยครั้งหนึ่ง
- **mock เต็มฉบับสัปดาห์ละครั้ง ไม่ใช่ทุกวัน** — ทำถี่เกินไปคือวัดความล้า ไม่ใช่วัดความรู้

---

## 5. สิ่งที่ตั้งใจ *ไม่* สร้าง

| ไม่สร้าง | ใช้อะไรแทน |
|---|---|
| Reader + คลังบทอ่านทั่วไป | Du Chinese / Pleco |
| ตัวจำลอง UI iBT | chinesetest.cn มีให้ลองฟรี |
| Psychometrics (p-value, point-biserial) | ต้องการ n≥20 ต่อข้อ · ผู้ใช้น้อยยังไม่ถึง |
| RSVP / Pacer / canvas เขียนมือ | สอบ iBT ไม่ต้องเขียนมือ · หลักฐานว่าช่วยอ่านจีนอ่อน |
| ตรวจงานเขียนอัตโนมัติเต็มระบบ (เฟสแรก) | ระบบถ่ายทอด (D11) — ระบบสร้างพรอมต์ให้ เจ้าของก๊อปไปถามเองแล้ววางผลกลับ |

---

## 6. ความไม่แน่นอนที่ยังค้างอยู่ ⚠️

**ต้องแก้ก่อนเชื่อตัวเลขใดๆ ในแผน**

- [ ] **ระดับจริงของน้อง** — ตัวเลข "HSK4 / 3-4 ชม.ต่อวัน" เป็นการ**เดา** ยังไม่ได้ถามน้อง → ถามครูที่สอนอยู่ (5 นาที) + ให้น้องทำข้อสอบเก่า 1 ชุดจับเวลาจริง
- [ ] **เวลาจริงต่อวัน** — น้องเตรียมสอบเต็มเวลา อาจได้ 6+ ชม. ซึ่งดีกว่าที่แผนคำนวณไว้มาก
- [ ] **มีศูนย์สอบ iBT ในไทยรอบ 7 พ.ย. / 13 ธ.ค. ไหม** — ถ้าไม่มี แผนสองรอบใช้ไม่ได้
- [ ] **ค่าสอบจริง** — ประมาณหยาบ 1,500-2,500 บาท ยังไม่ยืนยัน
- [ ] **ปลายทางต้องการ 180 รวม หรือ 60 ต่อพาร์ท** — ถ้าเป็นอย่างหลัง กลยุทธ์ทุ่มพาร์ทเขียนใช้ไม่ได้
- [ ] **HSK 3.0 จะบังคับใช้เมื่อไหร่** — เช็ค chinesetest.cn ทุกวันจันทร์

---

## 7. ทรัพยากรที่มีอยู่แล้ว

- `~/Documents/จีน/H51001.pdf` … `H51332.pdf` — **ข้อสอบจริง 9 ชุด** ใช้ calibrate ได้
  ⚠️ **ลิขสิทธิ์** — ใช้ส่วนตัวได้ ห้ามนำเข้าฐานข้อมูลเวอร์ชันที่จะขาย
  ทุกอย่างที่ดึงมาจากไฟล์เหล่านี้ต้องตั้ง `source_type="official_past_paper"` และ `commercial_safe=False`
- น้องมีหนังสือเตรียมสอบ + ครูสอน
- `data/exam_corpus/` — ข้อสอบ 9 ชุดที่แยกโครงสร้างแล้ว (โจทย์/ตัวเลือก/เฉลย/สคริปต์ฟัง)
  ⚠️ `commercial_safe=false` · อยู่ใน .gitignore แล้ว ห้าม commit
  `synonym_sets.json` = กลุ่มคำใกล้เคียง 100 กลุ่มที่ดึงจากตัวเลือกจริงของ 阅读第一部分

---

## 8. โครงสร้างโปรเจกต์

```
hsk5-trainer/
├── config/           settings, urls, wsgi
├── core/
│   ├── models/       users · lexicon · srs · content · practice · diagnostics
│   ├── srs.py        ตัวจัดตารางทบทวน (SM-2 ปรับให้รู้จักวันสอบ)
│   ├── listening.py  แยกบทถอดเสียง 听力 จากข้อสอบจริงเป็นรายข้อ
│   ├── listen_drill.py   ฝึกฟังทีละข้อ · listen_explain.py ชี้ประโยคที่มีคำตอบ
│   ├── dictation.py  听写 — เทียบทีละตัวอักษร แยกเสียงพ้องออกจากตัวผิด
│   ├── weekly.py     สรุปรายสัปดาห์ เทียบกับสัปดาห์ก่อนเสมอ
│   ├── selection.py  Daily Drill Engine
│   ├── placement.py  แบบวัดระดับ (สุ่มคำ · ตรวจ · แปลผลเป็นค่าตั้งต้น)
│   ├── drill.py      ตัวประสานชุดข้อสอบกับหน้าเว็บ
│   ├── views.py      หน้าเว็บผู้เรียน — บางที่สุด ตรรกะอยู่ที่อื่น
│   ├── admin.py      ครบทุกตาราง — สำหรับเจ้าของ/ครูเท่านั้น ผู้เรียนไม่ได้ใช้
│   └── management/commands/  seed_hsk5 · import_vocab · make_learner
├── templates/        base + หน้าผู้เรียน
├── static/           app.css (ระบบสีชุดเดียว) · htmx · alpine (วางไว้ในเครื่อง ไม่มี CDN)
└── data/             seed JSON
```

---

## 9. คำสั่งที่ใช้บ่อย

```bash
source .venv/bin/activate
python manage.py migrate
python manage.py seed_hsk5          # ใส่คำศัพท์ + 近义词 + 完成句子 ตั้งต้น
python manage.py import_vocab       # นำเข้าคำศัพท์ 2,496 คำจาก data/vocab/hsk5_merged.json
python manage.py import_listening --apply   # เติมบทถอดเสียงให้ข้อฟัง แล้วเปิดใช้ข้อที่พร้อม
python manage.py make_icons         # สร้างไอคอน PWA (ทำครั้งเดียว ไฟล์อยู่ใน static/icons/)
python manage.py make_learner mint --name มิ้นท์ --exam-date 2026-11-07 --password '<ตั้งเอง>'
python manage.py createsuperuser
python manage.py runserver
python manage.py test               # ทดสอบ SRS + drill engine
```

---

## 10. สถานะ / ทำถึงไหนแล้ว

- [x] **Sprint 1** — scaffold, data model ครบ, admin, seed, tests
- [x] **Sprint 1.5** — ฐานข้อมูลคำศัพท์ 2,496 คำ จากลิสต์ทางการ 2012 + ข้อสอบจริง 9 ชุด
  `data/vocab/hsk5_merged.json` · คำที่ต้องให้ครูตรวจอยู่ใน `needs_review.json` · อ่าน `QUALITY_REPORT.md` ก่อนใช้
  ⚠️ สุ่มตรวจ 5% พบผิดจริง 2.4% **เกินเกณฑ์ 2% ที่ตั้งไว้** → ยังไม่ผ่านการรับรอง ต้องให้ครูตรวจก่อนใช้สอน
- [x] **Sprint 2 (บางส่วน)** — UI ผู้เรียนใช้งานได้จริงแล้ว: login · แบบวัดระดับ · ชุดฝึกรายวัน · คลังคำศัพท์ HSK1-6 · สถิติ
  นำเข้าคำศัพท์เข้าฐานข้อมูลแล้ว 2,206 คำ · เทสต์ 33 ตัวผ่าน
  ⚠️ ในจำนวนนี้ติดป้าย `needs_review` 879 คำ · `review:warn` 817 คำ · `human_verified` **0 คำ**
  = ยังไม่มีใครตรวจสักคำ ต้องให้ครูดูก่อนเชื่อคำแปล
  คำสั่งใหม่: `import_vocab` · `make_learner`
- [x] **Sprint 2.5** — หน้าเขียนเรียงความ 书写第二部分 + ระบบถ่ายทอดผลตรวจ (D11)
  ตรรกะสามชั้นอยู่ใน `core/essay.py` (นับ+ตัดสิน) · `core/essay_grader.py` (พรอมต์+แกะผลที่วางกลับ)
  คิวของเจ้าของระบบอยู่ที่ `/essay/queue/list/` · ความยินยอมเก็บที่ `LearnerProfile.essay_consent_at`
- [ ] Sprint 2 (ที่เหลือ) — ระบบทดสอบแยกพาร์ท + คำอธิบายเฉลย 882 ข้อ + หน้าคำเชื่อม
- [x] **Sprint 3** — พาร์ทฟังใช้งานได้แล้ว: ข้อสอบฟัง 225 ข้อ · 听写 724 ประโยค
  `core/listening.py` แยกบทถอดเสียงรายข้อ · `core/listen_drill.py` หน้าฝึกฟัง ·
  `core/listen_explain.py` ชี้ประโยคที่มีคำตอบ · `core/dictation.py` เทียบตัวอักษร
  เสียงมาจากตัวอ่านของเบราว์เซอร์ (`static/js/listen.js`) ไม่มีไฟล์เสียงในระบบ
  ⚠️ เหลืออีก 180 ข้อจาก 4 ชุด (H51327/328/330/332) ที่ **ไม่มีบทถอดเสียงในไฟล์** ยังเป็น draft
- [x] **Sprint 4** — หน้าความคืบหน้ากลุ่ม (`/progress/`) + สรุปรายสัปดาห์ (`/weekly/`)
- [~] Sprint 5 — **deploy ขึ้น Railway แล้ว** (ไม่ใช่ VPS ตาม D5 เดิม — Railway ถูกและง่ายกว่า)
  PWA ทำแล้ว (`/manifest.webmanifest` · `/sw.js` · `make_icons`)
  คำสั่ง `backup` เขียนแล้วและทดสอบ restore ผ่าน แต่ **ยังไม่ได้ตั้ง cron ให้รันเอง**

---

## 11. หลักการเขียนโค้ดในโปรเจกต์นี้

- ข้อความที่ผู้ใช้เห็นเป็น **ภาษาไทย** · ชื่อตัวแปร/ตาราง/ฟังก์ชันเป็น **อังกฤษ**
- ทุก model ที่เก็บเนื้อหาต้องมี `source_type`, `commercial_safe`, `standard`
- ตรรกะ SRS และการเลือกข้อสอบอยู่ใน `core/srs.py` และ `core/selection.py` เท่านั้น **ห้ามกระจายเข้า view**
- เขียน test คู่กับตรรกะเสมอ — โดยเฉพาะ scheduler ที่ผิดแล้วเงียบ
- ห้ามเชื่อเฉลยที่ AI สร้างโดยไม่มีทางให้ผู้ใช้กดว่า "เฉลยผิด" → `Question.status = suspended`
